manifest: ./lzc-manifest.yml
pkgout: ./
icon: ./icon.png

compose_override:
  services:
    hermes-webui:
      cap_add:
        - SYS_ADMIN
      devices:
        - /dev/fuse:/dev/fuse
      volumes:
        - /data/playground/docker.sock:/var/run/docker.sock
